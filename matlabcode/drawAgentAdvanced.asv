function y = drawAgent(taskPoints, workerPoints, taskValues, )
taskColor = 'black';
workerColor = 'red';
%disp(tasks(:,1))
%disp(tasks(:,2))
%for i = [0:length(tasks)]
scatter(taskPoints(:,1), taskPoints(:,2), 'filled');
hold on;
%scatter(workers(:,1), workers(:,2), 'filled');
%for i = 1:length(tasks)
    
%end
for j = 1:length(workerPoints)
    drawCircle(workerPoints(j,:),workerRanges(j));
end
axis equal;
hold off;